#!/bin/bash

value=$(( ( RANDOM % 32 )  + 1 ))
verbose=1
size="4K"
async="Y"
mode2d="N"
batch="N"
merge="Y"
def=""

# read the options
TEMP=`getopt -o n:s:a::b::v:2::m::d --long value:,size:,async::,batch::,verbose:,2dmode::,merge::,defaults -n 'sendpar' -- "$@"`
eval set -- "$TEMP"

while true ; do
    case "$1" in
		-n | --value)
			value="$2" ; shift 2 ;
			echo -n "$value" | sudo tee /sys/kernel/debug/dmatest/dvc_value &> /dev/null ;;

		-s | --size)
			size="$2" ; shift 2 ;
			echo -n "$size" | sudo tee /sys/kernel/debug/dmatest/glob_size &> /dev/null ;;
		
		-a | --async)

			if [ ! -z "$2" ]; then
				async="$2"
			else
				old=$(sudo cat /sys/kernel/debug/dmatest/async_mode)

				if [ "$old" == "Y" ]; then
					async="N"
				else
					async="Y"
				fi
			fi
			
			echo -n "$async" | sudo tee /sys/kernel/debug/dmatest/async_mode &> /dev/null
			shift 2;;

		-m | --merge)

			if [ ! -z "$2" ]; then
				merge="$2"
			else
				old=$(sudo cat /sys/kernel/debug/dmatest/merge_vs_split)
				
				if [ "$old" == "Y" ]; then
					merge="N"
				else
				    merge="Y"
				fi
			fi
			
			echo -n "$merge" | sudo tee /sys/kernel/debug/dmatest/merge_vs_split &> /dev/null
			shift 2;;
		
		-b | --batch)

			if [ ! -z "$2" ]; then
				batch="$2"
			else
				old=$(sudo cat /sys/kernel/debug/dmatest/batch_mode)
				
				if [ "$old" == "Y" ]; then
					batch="N"
				else
				    batch="Y"
				fi
			fi
			
			echo -n "$batch" | sudo tee /sys/kernel/debug/dmatest/batch_mode &> /dev/null
			shift 2;;
		
		-2 | --2dmode)
			
			if [ ! -z "$2" ]; then
			    mode2d="$2"
			else
				old=$(sudo cat /sys/kernel/debug/dmatest/2d_mode)
				
				if [ "$old" == "Y" ]; then
					mode2d="N"
				else
				    mode2d="Y"
				fi
			fi
			
			echo -n "$mode2d" | sudo tee /sys/kernel/debug/dmatest/2d_mode &> /dev/null
			shift 2;;

		-v | --verbose)
			verbose="$2" ; shift 2 ;
			echo -n "$verbose" | sudo tee /sys/kernel/debug/dmatest/verbose &> /dev/null ;;

		-d | --defaults)
			def="Y" ; shift ;;
		
		--) shift ; break ;;
		*) echo "Error parsing opts, aborting." ; exit 1 ;;
	esac
done

if [ ! -z "$def" ]; then

	echo -n "$value" | sudo tee /sys/kernel/debug/dmatest/dvc_value &> /dev/null
	echo -n "$size" | sudo tee /sys/kernel/debug/dmatest/glob_size &> /dev/null
	echo -n "$async" | sudo tee /sys/kernel/debug/dmatest/async_mode &> /dev/null
	echo -n "$mode2d" | sudo tee /sys/kernel/debug/dmatest/2d_mode &> /dev/null
	echo -n "$verbose" | sudo tee /sys/kernel/debug/dmatest/verbose &> /dev/null
	echo -n "$batch" | sudo tee /sys/kernel/debug/dmatest/batch_mode &> /dev/null
	echo -n "$merge" | sudo tee /sys/kernel/debug/dmatest/merge_vs_split &> /dev/null
	
fi

